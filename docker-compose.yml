services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_GOOGLE_CLIENT_ID: ${VITE_GOOGLE_CLIENT_ID}
        VITE_TEST_MODE: "false"
    ports:
      - "8080:8080"
    environment:
      - VITE_TEST_MODE=false
    env_file:
      - .env