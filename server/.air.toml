root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/server ./cmd/server/main.go"
  bin = "./tmp/server"
  include_ext = ["go"]
  exclude_dir = ["tmp", "bin"]
  delay = 500
  stop_on_error = true
  send_interrupt = true

[log]
  time = true
